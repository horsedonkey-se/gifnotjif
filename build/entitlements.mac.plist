<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Used only when the app is signed with a Developer ID, which turns the hardened
  runtime on. The first three are what Electron needs to run V8 at all. The
  fourth is what lets the app spawn ffmpeg: without it the hardened runtime
  applies library validation to the child and refuses.

  This file is also passed as entitlementsInherit, so ffmpeg inherits it.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
